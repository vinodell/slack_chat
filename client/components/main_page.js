import React from 'react'
// import { useSelector } from 'react-redux'

const MainPage = () => {
  // const { messageHistory } = useSelector(s => s.message)
  return <div>this is main page</div>
}

export default MainPage
